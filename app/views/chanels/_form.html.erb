<%= simple_form_for(@chanel) do |f| %>
<div class="field">
  <div class="control">
    <%= f.input :chanel, input_html: {class: 'input'}, wrapper: false, label_html: {class: 'label'}  %>
  </div>
</div>

<%= f.button :submit,  class: 'button is-info' %>
<% end %>