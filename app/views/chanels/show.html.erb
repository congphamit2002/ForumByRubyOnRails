<div class="level">
  <div class="level-left">
  <h1 class="title is-2"><%= @chanel.chanel %></h1>
  </div>
  <div class="level-right">
  <% if has_role?(:admin) %>
    <%= link_to 'Edit channel', edit_chanel_path(@chanel), class:"button" %>
  <% end %>
  </div>
</div>
<hr />

<div class="columns">
 <%= render 'shared/discussion' %>
 <%= render 'discussions/sidebar' %>
</div>